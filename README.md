# core_l_h

MVP implementation scaffold (Next.js App Router + TypeScript). AWS Amplify Gen2/AppSync integration will be added next.

## Requirements

- Node.js 22+

## Dev

- Install deps: `npm install`
- Run dev server: `npm run dev`

## Notes

- Spec SSOT is maintained in `../SPEC_SSOT.md` (repo root workspace).
